

## 下一步待做

### 新频道接入
- [x] 直播频道（test_直播频道.py）
- [x] 精选频道（test_精选频道.py）
- [ ] 电视剧频道 + 其他频道 → 只需新建 30 行 py 配置

### 引擎优化
- [ ] template_inspector.inspect() 接收 public_checks + exclusive_checks 合并传递（当前已修复，但需验证排他检查在 AI 调用中正确合并）
- [ ] 在 AI 图片选择逻辑中，crop_marked→crop→全屏标记→原始截图 → 若裁剪+绿框失败自动降级到全屏标记，需在 TemplateInspector 中统一
- [ ] **ai.ask_focus() 布局匹配兜底**：当 XML 反查不到焦点所在模板名称时，加载当前频道全部有效模板（跳过 expected:false）的布局描述列表，注入 system_prompt 让 AI 按绿框裁剪图的视觉布局与描述列表匹配，以匹配值最高的模板名作为结果返回（合并进原始问题，不额外增加 API 调用）

### 长期待办
- ⭐⭐⭐	智能Diff (#1)	ai_service 现有 compare_images 加1个方法，改动最小，价值最高
- ⭐⭐⭐	加载完成检测 (#2)	替代硬等5秒，弱网场景刚需，改 appium_nav.py 即可
- ⭐⭐	AI模板分类 (#3)	解决直播居中模板无标题标记的痛点，作为 template_region_extractor 兜底
- ⭐⭐	异常根因分析 (#5)	合并截图+日志推理，减少人工排查时间
- ⭐	Smart Baseline (#4)	依赖Diff先做好，再上回归对比
- ⭐	趋势分析 (#6)	跑一段时间积累数据后再做，别过早优化